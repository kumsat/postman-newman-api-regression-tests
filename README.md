# Postman + Newman API Regression Tests (ReqRes)

This project contains an automated API regression suite for the public **ReqRes** API using:

- Postman collection
- Newman CLI runner
- Environment configuration for base URL

## How to Run

1. Clone the repository and go to the folder:

```bash
cd postman-newman-api-regression-tests

